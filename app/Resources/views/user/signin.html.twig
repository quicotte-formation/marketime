{% extends 'base.html.twig' %}

{% block body %}
    <h1>Sign in</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create account" />
    {{ form_end(form) }}
{% endblock %}
